# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.idea*

# work files
accountservice-linux-amd64
accounts.db
accountservice
healthchecker-linux-amd64

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out